import { Router } from "express";

const router = Router();

router.post("/", () => {});

router.get("/", () => {});

router.get("/:id", (id) => {});

router.put("/:id/", (id) => {});

router.delete("/:id", (id) => {});
